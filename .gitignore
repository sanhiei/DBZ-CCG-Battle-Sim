node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
# Scraped image cache (large); catalog JSON in data/ IS committed
data/images/
# Card faces sliced out of the TTS atlases (large, and not ours to redistribute)
data/images-tts/

# Tesseract OCR language model (5MB, downloadable — not source)
eng.traineddata
**/eng.traineddata
coverage/
*.log

# Downloaded TTS atlases (large, transient)
data/atlas-cache/

# Per-card OCR records from the TTS pass (regenerable; merged file is committed)
data/ocr-tts/

# Lackey plugin download cache (parsed output data/cards.lackey.json IS committed)
data/lackey/
data/vision-batches/
data/verify-batches/
